services:
  dashboard:
    build: .
    restart: unless-stopped
    volumes:
      - /opt/lottery-analytics/data:/app/data:ro
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8501:8501"
